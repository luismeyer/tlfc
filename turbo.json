{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "compile": {
      "dependsOn": ["^compile"],
      "inputs": ["src/**/*"],
      "outputs": ["lib/**/*"]
    },
    "compile:watch": {
      "inputs": ["src/**/*"],
      "outputs": ["lib/**/*"],
      "cache": false,
      "persistent": true
    }
  }
}
