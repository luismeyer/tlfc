{
  "name": "root",
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "compile": "turbo compile",
    "compile:watch": "turbo compile:watch",
    "dev": " turbo dev",
    "build": "turbo build"
  },
  "devDependencies": {
    "@changesets/cli": "^2.26.2",
    "turbo": "^1.10.12"
  }
}
