{
  "name": "tsls",
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "compile": "turbo compile",
    "compile:watch": "turbo compile:watch"
  },
  "devDependencies": {
    "turbo": "^1.10.7"
  }
}
